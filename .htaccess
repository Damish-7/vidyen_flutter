RewriteEngine On
RewriteBase /vidyen_flutter/

# ✅ Let API calls pass through — don't rewrite them
RewriteRule ^api/ - [L]

# Flutter web catch-all
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [QSA,L]